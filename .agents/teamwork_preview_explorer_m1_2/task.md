# Task: Analyze RTL Support, Arabic Typography & Form Contextual Directionality

You are Explorer 2. Your task is to analyze RTL support and styling conventions in the Next.js workspace under `frontend/`.

## Instructions
1. Check how the application handles language direction (RTL/LTR) - is there an html wrapper, context provider, or directional classes?
2. Map all styling directives to CSS Logical Properties for padding, margins, borders, and position alignment (e.g. `margin-inline-start`, `padding-inline-end`).
3. Check font configurations (Cairo/Tajawal), confirm typography setup, and verify how to apply minimum 16px font-size and line-height 1.8.
4. Plan how forms and input fields will use `dir="auto"` for contextual directionality.
5. Write your findings to `findings.md` in your working directory.
6. Do NOT modify any code.
