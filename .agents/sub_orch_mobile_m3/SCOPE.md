# Scope: React Native Expo Mobile App

## Architecture
- React Native (Expo) app in `mobile/`.
- Must connect to the FastAPI backend endpoints.
- UI rules: Arabic typography (Cairo/Tajawal >= 16px, line-height 1.8), CSS logical properties, dir="auto" on forms.

## Milestones
| # | Name | Scope | Dependencies | Status |
|---|------|-------|-------------|--------|
| 1 | Initialize Expo App | Setup Expo boilerplate in `mobile/` and basic screen UI connecting to backend API | none | IN_PROGRESS |
| 2 | Compile Export | Run and verify `npx expo export` builds successfully without errors | 1 | PLANNED |

