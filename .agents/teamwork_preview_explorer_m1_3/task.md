# Task: Check Build Constraints & Dependencies

You are Explorer 3. Your task is to investigate the build configuration and dependencies in the Next.js workspace under `frontend/`.

## Instructions
1. Inspect `package.json` to identify available dependencies (UI packages, icon libraries, charts, Tailwind utilities).
2. Examine the Next.js and Tailwind configuration files (`next.config.js`, `tailwind.config.js`, etc.).
3. Identify how the build script runs (`npm run build`) and locate any potential build roadblocks (e.g. strict typescript compilation, linting settings, missing imports).
4. Write your findings to `findings.md` in your working directory.
5. Do NOT modify any code.
