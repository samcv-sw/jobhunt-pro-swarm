# ──────────────────────────────────────────────────────────────────────────────
# JobHunt Pro - Optimized Dependencies (v3.0.0)
# Deduplicated, organized, and production-ready
# ──────────────────────────────────────────────────────────────────────────────

# ── Core Framework ────────────────────────────────────────────────────────
fastapi>=0.118,<1.0
uvicorn[standard]>=0.38
granian==1.5.2
httpx==0.28.1
a2wsgi==1.10.10
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3

# ── Database & ORM ────────────────────────────────────────────────────────
SQLAlchemy==2.0.23
psycopg==3.3.4
psycopg-pool==3.3.1
psycopg2-binary==2.9.12
asyncpg>=0.29
aiosqlite==0.19.0
# libsql-experimental removed from default install: requires a Rust toolchain to build on Windows
# and is an optional (Turso edge DB) dependency -- the code falls back to SQLite/Neon automatically.
# Install manually with a Rust toolchain only if you use Turso:  pip install libsql-experimental

# ── Authentication & Security ────────────────────────────────────────────
bcrypt==4.1.1
itsdangerous==2.2.0
python-dotenv==1.2.2
pydantic==2.12.5
pydantic-ai==2.2.0

# ── Caching & Performance ────────────────────────────────────────────────
redis==7.4.0
fastapi-cache2==0.2.2
orjson==3.11.9

# ── Rate Limiting ────────────────────────────────────────────────────────
slowapi==0.1.9
Flask-Limiter==3.7.0

# ── Async & Job Queue ────────────────────────────────────────────────────
procrastinate==3.9.0
celery>=5.3
groq>=0.9
filelock>=3.13
langgraph==1.2.6
langgraph-checkpoint-postgres==3.1.0

# ── Web Scraping & Browser Automation ────────────────────────────────────
duckduckgo-search==8.1.1
curl_cffi==0.15.0
camoufox[geoip]==0.5.4

# ── Payment Processing ────────────────────────────────────────────────────
stripe==15.3.0

# ── Email & Messaging ────────────────────────────────────────────────────
dkimpy==1.1.8
dnspython==2.8.0
aiosmtpd==1.4.6

# ── Templating & Rendering ──────────────────────────────────────────────
jinja2==3.1.2

# ── System & Utilities ────────────────────────────────────────────────────
psutil==7.2.2
h2==4.3.0
watchdog==6.0.0

# ── File Processing ──────────────────────────────────────────────────────
openpyxl==3.1.5
fpdf2>=2.7
beautifulsoup4>=4.12
pdfplumber==0.11.9

# -- Testing & Quality Assurance ------------------------------------------
# Moved to requirements-dev.txt -- not installed in the production image.
# Run: pip install -r requirements-dev.txt

# --------------------------------------------------------------------------
# Production dependencies only. Test/dev tools live in requirements-dev.txt.
eventlet==0.37.0
# --------------------------------------------------------------------------
