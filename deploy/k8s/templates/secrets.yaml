apiVersion: v1
kind: Secret
metadata:
  name: jobhunt-secrets
  labels:
    app: jobhunt-pro
type: Opaque
stringData:
  groq-api-key: "REPLACE_WITH_YOUR_GROQ_API_KEY"
  stripe-api-key: "REPLACE_WITH_YOUR_STRIPE_KEY"
  pg-password: "REPLACE_WITH_STRONG_PASSWORD"
  jwt-secret: "REPLACE_WITH_STRONG_JWT_SECRET"
