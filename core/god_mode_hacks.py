# ALL FUNCTIONS DISARMED — original code was triggering spam filters. Restore at your own risk.

import logging
from typing import Optional

logger = logging.getLogger(__name__)


def inject_stealth_payload(html: str, company: str, job_title: str) -> str:
    """DISARMED — was injecting hidden keyword-stuffed divs. Pass-through only."""
    return html


def inject_bgp_hijack_spoof(html: str, company: str, ceo_name: str = "CEO") -> str:
    """DISARMED — was spoofing internal corporate forward headers. Pass-through only."""
    return html


def get_bgp_hijack_subject(original_subject: str) -> str:
    """DISARMED — was faking internal forward subjects. Pass-through only."""
    return original_subject


def generate_proof_of_work_link(company: str, candidate_name: str) -> str:
    """DISARMED — was generating deceptive portfolio links. Pass-through only."""
    return ""
